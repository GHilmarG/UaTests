function [a,as,ab]=DefineMassBalance(Experiment,time,s,b,S,B,coordinates,connectivity,TRIxy,rhow,rho,CtrlVar)

    Nodes=length(s);
    ab=zeros(Nodes,1);    
    as=zeros(Nodes,1);
    a=as+ab;
        
    
end
